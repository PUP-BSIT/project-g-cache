export const environment = {
  production: false,
  // Use production API URL for development (or use proxy in proxy.conf.json)
  apiUrl: 'https://api.pomodify.site/api/v1',
  // Enable mock backend temporarily until deployed backend cache is fixed
  useMockBackend: true, // Changed to true for local testing
};
