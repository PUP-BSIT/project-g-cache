# Live AI Test Profile - Uses real Google GenAI client
# Requires GOOGLE_API_KEY environment variable to be set

# Docker Postgres datasource for integration tests
spring.datasource.url=jdbc:postgresql://localhost:55432/pomodifydb_test
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=Qazplm891251

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.flyway.enabled=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Enable real AI adapter
ai.enabled=true

# JWT test configuration
jwt.secret=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=2592000000

# Disable unused services
fcm.service-account=
spring.mail.host=
spring.mail.port=0

# Google OAuth2 Test Configuration
spring.security.oauth2.client.registration.google.client-id=test-client-id
spring.security.oauth2.client.registration.google.client-secret=test-client-secret
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8081/login/oauth2/code/google
spring.security.oauth2.client.provider.google.token-uri=http://localhost:8081/oauth2/token
spring.security.oauth2.client.provider.google.authorization-uri=http://localhost:8081/oauth2/authorize

# Verbose logging for AI rotation debugging
logging.level.com.pomodify.backend.infrastructure.adapter.ai=DEBUG
