
# Docker Postgres datasource for integration tests
spring.datasource.url=jdbc:postgresql://localhost:55432/pomodifydb_test
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=Qazplm891251

# Let Hibernate create schema for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Avoid noisy logs
logging.level.org.hibernate.SQL=warn
logging.level.org.springframework.jdbc=warn
logging.level.org.springframework.security=warn

# Do not attempt to initialize Firebase if not configured
fcm.service-account=

# JWT test configuration
jwt.secret=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=2592000000

# Disable unused email configuration in tests
spring.mail.host=
spring.mail.port=0
spring.mail.username=
spring.mail.password=
