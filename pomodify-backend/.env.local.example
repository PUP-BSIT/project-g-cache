# Copy to .env.local and adjust values for local dev
# Run: export $(grep -v '^#' .env.local | xargs)

# Profile
SPRING_PROFILES_ACTIVE=local

# If using Postgres locally, override datasource in application-local.properties or set:
# DB_URL=jdbc:postgresql://localhost:5432/pomodify
# DB_USERNAME=postgres
# DB_PASSWORD=postgres

# JWT (dev defaults are set; override as needed)
JWT_SECRET=local-secret
JWT_ACCESS_EXPIRATION=3600
JWT_REFRESH_EXPIRATION=604800

# Firebase (leave empty to disable locally)
FCM_SERVICE_ACCOUNT=
